language: python
python:
  - "3.7"
install:
  - pip install .
before_script:
  - pip install flake8 pytest-cov coveralls
script:
  - flake8 combat_simulator/ tests/
  - py.test --cov=combat_simulator/ tests/
after_success:
  - coveralls
